/**
 * @summary
 * External API routes (public access)
 * Routes for authentication, registration, and public endpoints
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

export default router;
