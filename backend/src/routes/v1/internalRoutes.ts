/**
 * @summary
 * Internal API routes (authenticated access)
 * Routes for authenticated user operations
 *
 * @module routes/v1/internalRoutes
 */

import { Router } from 'express';

const router = Router();

export default router;
